[project]
name = "hbicproc-pipeline"
version = "0.1.0"
description = "Minimal fMRI preprocessing workflow orchestrator."
requires-python = ">=3.8"

[project.scripts]
pipeline = "pipeline.cli:main"
annotate_protocol = "pipeline.annotate_protocol:main"
